const BackgroundHeading = () => {
  return (
    <h1 className="absolute top-8 left-1/2 -translate-x-1/2 text-[12vw] sm:text-[12vw] md:text-[120px] font-bold text-black/5 uppercase tracking-[0.2em] pointer-events-none select-none z-0">
      TODOApp
    </h1>
  );
};

export default BackgroundHeading;
